{
  "name": "webhook-intake-poc",
  "version": "0.1.0",
  "description": "Small stateful webhook intake POC for testing webhook senders.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.3"
  },
  "engines": {
    "node": ">=18"
  }
}
